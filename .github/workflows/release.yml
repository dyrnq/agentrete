name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: agentrete

jobs:
  test:
    name: Test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: test-release-cargo-${{ hashFiles('Cargo.lock') }}
      - name: Run tests
        run: cargo test
  build:
    name: Build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.target }}-cargo-${{ hashFiles('Cargo.lock') }}

      - name: Build (release)
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package binary
        shell: bash
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          BIN="${{ env.BINARY_NAME }}-${TAG}-${{ matrix.target }}"
          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            BIN="${BIN}.exe"
          fi
          cp target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}${{ contains(matrix.target, 'windows') && '.exe' || '' }} "$BIN"
          sha256sum "$BIN" > "${BIN}.sha256"
          echo "artifact=${BIN}" >> $GITHUB_OUTPUT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: ${{ env.BINARY_NAME }}-*

# TODO: enable after crate is ready
#  cargo-publish:
#    name: Publish to crates.io
#    needs: build
#    runs-on: ubuntu-24.04
#    steps:
#      - uses: actions/checkout@v4
#      - uses: dtolnay/rust-toolchain@stable
#      - name: Check if version already published
#        id: check
#        run: |
#          TAG="${GITHUB_REF#refs/tags/}"
#          VERSION="${TAG#v}"
#          echo "version=$VERSION" >> $GITHUB_OUTPUT
#          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://crates.io/api/v1/crates/agentrete/$VERSION" 2>/dev/null || echo "000")
#          if [ "$STATUS" = "200" ]; then
#            echo "Version $VERSION already published to crates.io"
#            echo "published=true" >> $GITHUB_OUTPUT
#          else
#            echo "Version $VERSION not yet published"
#            echo "published=false" >> $GITHUB_OUTPUT
#          fi
#      - name: Publish to crates.io
#        if: steps.check.outputs.published != 'true'
#        env:
#          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
#        run: cargo publish

  release:
    name: GitHub Release + npm publish
    needs: [build]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        shell: bash
        run: |
          mkdir -p release-assets
          find artifacts -type f -exec cp {} release-assets/ \;
          cd release-assets
          sha256sum * > sha256sums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          files: release-assets/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org/

      - name: Download platform binaries for npm
        shell: bash
        run: |
          mkdir -p npm/bin
          for dir in artifacts/*/; do
            platform=$(basename "$dir")
            # Find the actual binary file (skip .sha256 files)
            for f in "$dir"/*; do
              case "$f" in *.sha256) continue ;; esac
              binname="agentrete-${platform}"
              case "$platform" in
                *windows*) binname="${binname}.exe" ;;
              esac
              cp "$f" "npm/bin/$binname" 2>/dev/null || true
            done
          done
          chmod +x npm/bin/* 2>/dev/null || true
          ls -la npm/bin/

      - name: Update npm version from tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cd npm
          node -e "const p=require('./package.json');p.version='$VERSION'; require('fs').writeFileSync('package.json', JSON.stringify(p,null,2)+'\n')"
          echo "npm package version: $(node -e "console.log(require('./package.json').version)")

      - name: Check npm version
        id: npm-check
        run: |
          VERSION=$(node -e "console.log(require('./npm/package.json').version)")
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://registry.npmjs.org/agentrete/$VERSION" 2>/dev/null || echo "000")
          if [ "$STATUS" = "200" ]; then
            echo "Version $VERSION already published to npm"
            echo "published=true" >> $GITHUB_OUTPUT
          else
            echo "Version $VERSION not yet published"
            echo "published=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish to npm
        if: steps.npm-check.outputs.published != 'true'
        run: |
          cd npm
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
